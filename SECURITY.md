# Security

This is a hands-on learning lab using synthetic/mock data only — no real device,
tenant, or ticket data is included. Do not commit real `.env` values, tenant IDs,
or connection strings. Report concerns via a GitHub issue.
